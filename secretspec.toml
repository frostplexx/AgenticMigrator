[project]
name = "agentic-migrator"
revision = "1.0"

[providers]
op = "onepassword://my@DevVault"

[profiles.default]
LLM_API_KEY = { description = "API key for the LLM endpoint (chat-ai.academiccloud.de)", required = true, providers = ["op"] }
